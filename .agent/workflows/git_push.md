---
description: Auto-commit and push changes to git
---
1. Add all changes
// turbo
2. Commit with message "update" if no message provided
// turbo
3. Push to current branch

```bash
git add .
git commit -m "update"
git push
```
